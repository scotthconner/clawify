# =============================================================================
# Clawify TEE Environment Configuration
# =============================================================================
# This file defines all environment variables needed to one-shot bootstrap
# OpenClaw inside a TEE (Trusted Execution Environment).
#
# SECURITY NOTE: Variables are categorized by sensitivity:
# - PRIVATE: Never exposed, stays inside TEE only
# - INTERNAL: Used by OpenClaw but not shared externally
# - PUBLIC: Can be revealed/shared as needed
# =============================================================================

# -----------------------------------------------------------------------------
# WALLET CONFIGURATION (PRIVATE - NEVER EXPOSE)
# -----------------------------------------------------------------------------
# The mnemonic phrase for the crypto wallet. OpenClaw will derive the wallet
# address from this but will NEVER reveal the mnemonic or private key.
MNEMONIC="your twelve or twenty-four word mnemonic phrase goes here"

# -----------------------------------------------------------------------------
# LLM PROVIDER (INTERNAL)
# -----------------------------------------------------------------------------
# Anthropic API key for Claude models (recommended for OpenClaw)
ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# -----------------------------------------------------------------------------
# TELEGRAM CHANNEL (INTERNAL)
# -----------------------------------------------------------------------------
# Bot token from @BotFather
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# Comma-separated list of Telegram user IDs allowed to interact with the bot
# Get your ID by messaging @userinfobot or checking gateway logs
TELEGRAM_ALLOWED_FROM=123456789

# -----------------------------------------------------------------------------
# AGENT IDENTITY (PUBLIC)
# -----------------------------------------------------------------------------
# Name of the agent as it identifies itself
AGENT_NAME=Clawify

# Theme/personality description for the agent
AGENT_THEME=a secure crypto wallet assistant running in a TEE

# Emoji used by the agent
AGENT_EMOJI=🦞

# -----------------------------------------------------------------------------
# GATEWAY CONFIGURATION (INTERNAL)
# -----------------------------------------------------------------------------
# Port for the OpenClaw gateway (default: 18789)
GATEWAY_PORT=18789

# Bind address: "loopback" for localhost only, "0.0.0.0" for all interfaces
GATEWAY_BIND=loopback

# -----------------------------------------------------------------------------
# OPTIONAL: WEB SEARCH (INTERNAL)
# -----------------------------------------------------------------------------
# Brave Search API key for web search capability (optional)
# BRAVE_SEARCH_API_KEY=your-brave-api-key

# -----------------------------------------------------------------------------
# OPTIONAL: ADDITIONAL WALLET SETTINGS (PUBLIC)
# -----------------------------------------------------------------------------
# Default chain ID for transactions (e.g., 1 for Ethereum mainnet)
# DEFAULT_CHAIN_ID=1

# RPC endpoint for blockchain interactions
# RPC_URL=https://eth.llamarpc.com
